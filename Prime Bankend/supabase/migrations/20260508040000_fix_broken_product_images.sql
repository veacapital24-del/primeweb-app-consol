-- Already applied to remote (see supabase_migrations.schema_migrations).
-- Local file restored as a stub after the original was lost from disk.
-- No-op: idempotent comment so 'supabase migration repair --status applied'
-- can find the file without re-executing any DDL.
select 1 where false;
